import React from "react";

const Sidebar: React.FC = () => {
  return (
    <aside className="flex w-full max-w-[220px] flex-col items-center gap-4">
      {/* Sidebar content removed - avatar image was appearing misplaced */}
    </aside>
  );
};

export default Sidebar;
